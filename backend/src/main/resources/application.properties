# MongoDB connection (default: local MongoDB)
spring.data.mongodb.uri=mongodb://localhost:27017/car-data
# Optional: override with env var SPRING_DATA_MONGODB_URI

# Server port (default 8080) - uncomment to change
# server.port=8080

# Logging
logging.level.org.springframework=INFO
logging.level.lk.amila=DEBUG

